services:
  - type: web
    name: career-ujjawalata
    runtime: python
    buildCommand: pip install -r requirements.txt && python generate_data.py && python train_model.py
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
